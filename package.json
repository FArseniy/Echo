{
  "name": "echo",
  "version": "0.1.0",
  "private": true,
  "description": "Echo temporary chat without registration.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3",
    "supertest": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
